# Launch Notes

A small React app with a local Node preview server and Vercel-friendly static files.

## Run Locally

```sh
node server.js
```

Then open http://localhost:5173.

## Deploy

This app can be deployed to Vercel as a static project. No build step is required.
# launch-notes-react-live
A small React launch checklist app.
